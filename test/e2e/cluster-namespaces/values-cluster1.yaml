global:
  useClusterNamespaces: true
  clusters:
    - name: c1
    - name: c2

scheduler:
  enabled: true
  securityContext:
    runAsUser: 1000

agent:
  enabled: true
  securityContext:
    runAsUser: 1000

clusters:
  enabled: true
